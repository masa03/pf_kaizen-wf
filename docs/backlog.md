# 改善提案システム — 要件バックログ

ミーティング等で決まった追加要件をここに蓄積する。
詳細仕様策定時に `changes/` へ変更提案（proposal）を作成し、開発中セクションへ移動する。
実装完了・spec/マージ後に完了セクションへ移動する。

**前提**: v1（spec/ v10.2）の機能が完成していること

---

## 検討中

ミーティングで決まった要件を詳細に記述するセクション。
「§Xの変更提案を作成して」で changes/ に proposal を作成 → 開発中セクションへ移動。

### ~~§1 添付資料の多ファイル形式対応・容量表記~~ → 開発中セクションへ移動

### ~~§2 評価者の変更機能~~ → 開発中セクションへ移動

### ~~§3 回覧者（事前確認者）~~ → 開発中セクションへ移動

### §4 申請取消機能（優先度: 高）

申請者が回覧中・評価待ちの申請を自ら取り消せる機能を追加する。v1の取下げ（下書き・申請中のみ）を拡張する。

**取消可能なステータス**

| ステータス | v1（取下げ） | v2（申請取消） |
|-----------|-------------|---------------|
| 下書き | ○ | ○ |
| 申請中 | ○ | ○ |
| 回覧中 | — | **○（新規）** |
| 課長評価中 | × | **○（新規）** |
| 部長評価中 | × | **○（新規）** |
| 承認済 | × | × |
| 差戻 | × | ×（再編集可能なため不要） |

**取消時の処理**
- ステータスを「取下げ」に更新
- 現在の承認者（回覧者/評価者）へ取消通知メールを送信
- 既に入力済みの評価データがある場合: **保持**（削除しない。参考記録として残す）

**検討項目**
- 取消ボタンの配置: 閲覧画面 or ホーム画面の申請一覧
- 取消理由の入力（任意 or 必須）
- 取消後の再提出可否（差戻と同様に再編集→再提出できるか）

### §5 下書き保存機能（優先度: 高）

申請フォームで入力途中の内容を一時保存（下書き保存）できる機能を追加する。

**基本ルール**

| 項目 | 仕様 |
|------|------|
| v1のステータス | 「下書き」は既に定義済み。保存UIがないだけ |
| 保存タイミング | 申請者が「下書き保存」ボタンを押下時 |
| 保存先 | 改善提案メインリスト（ステータス=下書き） |
| 提出ボタンとの関係 | 下書き保存 → 後から編集 → 提出ボタンで正式提出 |

**動作**
- 新規申請時: 下書き保存ボタン押下 → ステータス=「下書き」でメインリストにレコード作成。Power Automateの申請通知フローはトリガーされない（条件: ステータス=申請中）
- 下書き編集時: 既存の下書きレコードを上書き更新
- 差戻後の再編集時: 差戻状態のレコードを編集中に下書き保存も可能とするか要検討

**検討項目**
- 下書き一覧の表示場所: ホーム画面 or SharePointビュー
- 下書きの有効期限（一定期間後に自動削除するか）
- バリデーション: 下書き保存時は必須チェックをスキップ or 最低限のチェックのみ
- 改善メンバー・改善分野実績の下書き保存: 子リストも一緒に保存する必要がある

### §6 集計・CSVダウンロード機能（優先度: 中）

給与振込のために、承認済の改善提案データをCSV形式でダウンロードする機能。

**出力項目（確認中）**
- 褒賞金額（FinalRewardAmount）
- 申請者氏名（ApplicantName）
- 申請者GID（ApplicantGID）
- 等級（Grade）— 評価データから取得
- 部署（Department / Division / Bu / Section）
- 改善テーマ（Theme）

**検討項目**
- 実装方式（Power Apps内ボタン → Power Automateフロー / SharePointのExportToExcel / 管理者画面に組み込み）
- フィルタ条件（期間指定、ステータス=承認済のみ）
- メンバー単位の出力（1提案に複数メンバーがいる場合）
- 出向者・派遣社員の集計区分
- アクセス権限（管理者のみ or 部長以上）

### §8 社員マスタサジェスト検索UI（優先度: 中）

改善メンバー追加・評価者変更など、社員マスタから人を選択するUI全般を「GID手入力」から「氏名サジェスト検索＋選択」方式に変更する。

**対象箇所**
- 改善メンバー追加UI（申請フォーム右カラム）
- 評価者変更UI（§2で追加予定）
- 回覧者追加UI（§3で追加予定）

**基本ルール**

| 項目 | 仕様 |
|------|------|
| 入力方式 | テキストボックスに氏名（部分一致）を入力 → サジェストリスト表示 → 選択 |
| 委任対策 | 社員マスタ15,000件のため、最低3文字入力後にフィルタ開始など制限が必要 |
| フォールバック | GID完全一致入力でも検索可能とする |

**検討項目**
- 委任制限回避方式（`Search`関数は委任非対応。コレクション事前読み込み or SharePoint REST API経由）
- サジェスト表示件数の上限（例: 最大10件）
- GID入力 or 氏名入力の判定ロジック

**備考**: §2（評価者変更）・§3（回覧者）の実装より先にこちらを実装し、共通UIパーツとして利用する。

### §7 提案プラン — v1からの繰り越し

**§7-1 ホーム画面（タスク 2-9）**（優先度: 中）
- 自分の申請一覧ギャラリー
- 承認待ち一覧ギャラリー（課長/部長ロールに応じて表示）
- ステータスフィルタ / 期間フィルタ
- 各行タップで閲覧画面 or 評価画面に遷移

**§7-2 管理者画面（タスク 2-10）**（優先度: 低）
- マスタCRUD（社員マスタ・改善分野マスタ・表彰区分マスタ）
- 全件閲覧
- エクスポート機能（CSVダウンロード機能 §6 と統合検討）

**§7-3 取下げ通知フロー（タスク 3-5）**（優先度: 中）
- 取下げ時の承認者通知メール
- v2の申請取消機能（§4）により、取消可能ステータスが拡張される

**§7-4 リマインダーフロー（タスク 3-6）**（優先度: 中）
- **頻度: 毎朝1回**（日次スケジュールトリガー）
- 承認期限超過の案件を検出（承認期限5日超過）
- 承認者へ督促メール送信
- 回覧者への督促も対象に含めるか要検討（§3）

**§7-5 承認履歴リスト（タスク 1-8）**（優先度: 中）
- 申請/承認/差戻/取下げ/再提出/回覧承認/回覧差戻のアクションログ記録

---

## 開発中

changes/ に変更提案（proposal）を作成済み。実装進行中。

- **§1 添付資料の多ファイル形式対応・容量表記** — [proposal](changes/v2-file-format/proposal.md)
- **§3 回覧者（事前確認者）** — [proposal](changes/v2-reviewer/proposal.md)
- **§2 評価者の変更機能** — [proposal](changes/v2-evaluator-change/proposal.md)

---

## 完了

spec/ にマージ済み。changes/archive/ に原本保存。

<!-- 例:
- §3 回覧者 — 2026-xx-xx完了 → [archive](changes/archive/2026-xx-xx-v2-reviewer/)
-->

（現在なし）
