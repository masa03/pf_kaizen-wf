# 4. Power Apps 画面設計

## 4.1 画面一覧

| No. | 画面名 | 対象ユーザー | 主な機能 |
|-----|-------|------------|---------|
| 1 | 申請フォーム（新規/編集） | 申請者 | 改善提案の入力・提出・修正・再提出 |
| 2 | 申請内容閲覧画面 | 申請者/課長/部長 | 申請内容の読み取り専用表示（プレビュー・承認時確認・登録後閲覧） |
| 3 | 評価画面（課長/部長共通） | 課長/部長 | 閲覧画面（上部）＋ 評価入力・承認/差戻（下部） ■ 評価ラジオボタンUI: 各評価軸は「極めて優秀(20,18) → 優れている(16,14) → 普通(10,8) → 物足りない(4,2)」の4段階×左右2択=8択のラジオボタンを横一列に配置する。①効果のみ、先頭に「金額算定 可/不可」スイッチを設け、ON時は各ボタン下に金額目安（≥10万 等）を表示する。 ■ 部長評価時のデフォルト値: 部長が評価画面を開いた際、課長の評価データ（各軸の点数・コメント）をデフォルト値として表示する。部長はそのまま承認するか、値を変更して再評価できる。差戻後の再提出時も、前回の評価データを参考値として表示する。 |
| ★4 | ホーム画面 | 全ユーザー | 自分の申請一覧、承認待ち一覧（ギャラリー表示） |

## 4.2 申請フォーム詳細

申請フォームは左右2カラム構成とし、左側に提案基本情報、右側に改善メンバー情報を配置する。

**左カラム - 基本情報**

| 項目 | 入力方式 | データソース/備考 |
|------|---------|----------------|
| 申請者メール | 自動表示（グレーアウト） | User().Email から取得 |
| 申請者GID | 自動表示（グレーアウト） | User().Emailから社員マスタを逆引きしてGIDを自動取得。該当なしの場合はエラー表示「社員情報が見つかりません」 |
| 表彰区分 | プルダウン | 改善提案/小集団 パール賞（3等）/小集団 銅賞（2等）/小集団 銀賞（1等）（マスタ参照） |
| TEC | 自動表示（グレーアウト） | 申請者GIDから社員マスタを参照して自動入力 |
| 部門 | 自動表示（グレーアウト） | 申請者GIDから社員マスタを参照して自動入力。空の場合あり `[v10]` |
| 部 | 自動表示（グレーアウト） | 申請者GIDから社員マスタを参照して自動入力 |
| 課 | 自動表示（グレーアウト） | 申請者GIDから社員マスタを参照して自動入力 |
| 係 | テキスト入力 | 手入力 |
| 改善テーマ | テキスト入力 | |
| 問題点 | 複数行テキスト | |
| 改善内容 | 複数行テキスト | |
| 改善完了日 | 日付ピッカー | |
| 改善分野（実績テーブル） | データテーブル + 数値入力 | 追加ボタンでプルダウンから分野を選択→選択した分野のフォームを表示→入力。複数分野を順次追加可能。テキスト型3分野（6S・ヒューマンエラー/環境/その他効果）も同一プルダウンから選択し、効果テキストを入力する統一UIとする |
| 添付ファイル | ファイルアップロード + 種別選択 | 任意、ファイルごとに種別（改善前/改善後/その他）を選択。複数アップロード可。改善前/改善後画像がない場合もある `[v10]` `[v10.2]` |

**改善分野の表示・入力仕様** `[update_category_02]`

> **単位表示の共通ルール**: マスタのUnitから「/月」を除去し、実績の単位のみを使用する。例: Unit="h/月" → 「円/h」、Unit="㎡/月" → 「円/㎡」
>
> **実績一覧ギャラリー（galCategories）の列構成**（金額算出型のみ）:
> - 分野名
> - 実績値（小数第1位まで表示） + 単位（例: 5.0 人/月）
> - 換算単価（カッコ付き・小サイズ・グレー、3桁区切り）（例: (760,232 円/人)）
> - 効果金額（3桁区切り）+ 円/月
>
> ※ CAT-06（リードタイム短縮）はConversionRateに仕掛かり金額を格納しているため、換算単価欄には仕掛かり金額が表示される
>
> **換算単価入力欄**: ラベルは「換算単価（円/（Unitの/より前））」で表示。値は3桁区切りで表示。CAT-06は非表示（従来通り）
>
> **リードタイム短縮（CAT-06）の効果金額算出**:
> - 効果金額 = 仕掛かり金額（WIPAmount）そのまま。実績値との乗算は行わない
> - 実績値（分/月）は入力欄を表示し、ActualValueに参考記録として保存する（計算には使用しない）
> - 換算単価入力欄は非表示（従来通り）
| プレビューボタン | ボタン | 申請内容閲覧画面を表示（提出前確認用） |
| 提出ボタン | ボタン | 確認ポップアップ表示後にLists登録 |

**右カラム - 改善メンバー**

> ※ v5からの変更: メンバー情報は改善メンバーリスト（別テーブル）で管理。各メンバーのGID入力時に社員マスタから氏名・所属場所を自動取得し、申請時点の情報を改善メンバーリストに保存する。

> ※ §8からの変更: GID手入力方式からサジェスト検索方式に変更。氏名またはGIDを入力すると社員マスタを検索し、候補一覧から選択してメンバーに追加する。

**改善メンバー追加UIパーツ（社員サジェスト検索）** `[§8]`

```
cntMemberSearch (縦・AutoLayout)
  ├── txtMemberSearch   … テキスト入力（氏名 or GID、プレースホルダー: 「氏名またはGIDを入力」）
  └── galMemberSuggest  … サジェストリスト（最大10件、候補あり時のみ表示）
        └── cntMemberSuggestRow (横・テンプレート行、36px)
              ├── lblMemberSuggestName  … 氏名（FillPortions=1、size=12）
              ├── lblMemberSuggestBu    … 部（幅80、size=11、グレー）
              └── lblMemberSuggestGID   … GID（幅90、size=10、グレー）
```

**検索ロジック**: 入力値が全数字 → GID完全一致検索（`Filter(社員マスタ, GID = 入力値 And IsActive = true)`）、それ以外 → 氏名前方一致検索（`Filter(社員マスタ, IsActive = true, StartsWith(EmployeeName, 入力値))`）。いずれも最大10件。

**候補選択時**: 重複チェック（MemberGID比較）・上限チェック（10名）を行い、`colMembers` に `{MemberGID, MemberName, MemberOffice, MemberCostUnit, SortOrder}` を追加。`txtMemberSearch` をリセット。

| 項目 | 入力方式 | データソース/備考 |
|------|---------|----------------|
| メンバー追加 | サジェスト検索 `[§8]` | 氏名またはGIDを入力→候補選択。退職者（IsActive=false）は除外。上限10名 |

## 4.3 申請内容閲覧画面（新規）

申請内容を読み取り専用で表示する画面。以下の場面で使用する。

（1）申請者が提出前にプレビューとして確認する場合
（2）申請者が申請登録後に自分の申請内容を閲覧する場合
（3）上長（課長/部長）が承認時に申請内容を確認する場合（評価画面の上部に組み込み）

| 表示項目 | 表示方式 | 備考 |
|---------|---------|------|
| リクエストID | テキスト（読取専用） | |
| 申請者情報 | テキスト（読取専用） | GID、氏名（在籍事業所・原価単位は画面非表示、メインリストにスナップショット保存） `[v9.3]` |
| 表彰区分 | テキスト（読取専用） | 改善提案/小集団 パール賞（3等）/小集団 銅賞（2等）/小集団 銀賞（1等） |
| TEC/部門/部/課/係 | テキスト（読取専用） | 5階層表示 `[v10]` |
| 改善テーマ | テキスト（読取専用） | |
| 問題点 | テキスト（読取専用） | 複数行表示 |
| 改善内容 | テキスト（読取専用） | 複数行表示 |
| 改善完了日 | テキスト（読取専用） | |
| 改善分野実績一覧 | データテーブル（読取専用） | 分野名、実績値（小数第1位）、換算単価（カッコ付き・単位は円/（Unitの/より前））、効果金額の一覧。表示形式は申請フォームのギャラリーと同一。CAT-06はConversionRateに仕掛かり金額が格納されており、そのまま表示する `[update_category_02]` |
| 効果金額合計 | テキスト（読取専用） | 自動算出された合計金額 |
| 6S・マナー効果 | テキスト（読取専用） | |
| 環境効果 | テキスト（読取専用） | |
| その他効果 | テキスト（読取専用） | |
| 改善メンバー一覧 | データテーブル（読取専用） | GID、氏名、在籍事業所、原価単位 |
| 改善前画像 | 画像埋め込み表示 | FileCategory="改善前" の画像を左カラムに直接表示 `[v10]` |
| 改善後画像 | 画像埋め込み表示 | FileCategory="改善後" の画像を右カラムに直接表示 `[v10]` |
| その他添付ファイル | リンク表示 | FileCategory="その他" のファイルをリンク一覧表示 `[v10]` |
| ステータス | バッジ表示 | 現在のステータス |
| 承認者情報 | テキスト（読取専用） | 課長・部長の氏名 |
| 評価結果（課長） | テキスト（読取専用） | 判定、等級、褒賞金額、4軸スコア（改善提案のみ）、コメント。ステータスが「承認済」「差戻」の場合で、かつ申請者本人のみに表示 `[v10]` `[v10.2]` |
| 評価結果（部長） | テキスト（読取専用） | 判定、等級、褒賞金額、4軸スコア（改善提案のみ）、コメント。部長評価がある場合で、かつ申請者本人のみに表示 `[v10]` `[v10.2]` |
| 最終褒賞金額 | テキスト（読取専用） | FinalRewardAmountを表示。承認済の場合で、かつ申請者本人のみに表示 `[v10]` `[v10.2]` |

## 4.4 評価画面詳細（課長/部長共通）

評価画面は上部に申請内容閲覧画面（読み取り専用）を組み込み、下部に評価入力欄を配置する。課長・部長画面は同一構造で、評価者種別のみ異なる。表彰区分（小集団 パール賞（3等）/小集団 銅賞（2等）/小集団 銀賞（1等））が設定されている場合は、評価入力欄（②独創性、③努力工夫、④応用範囲）を非表示またはグレーアウトし、承認ボタンのみ表示する。

| 評価項目 | 入力方式 | 点数算出ロジック |
|---------|---------|---------------|
| ①効果 | 先頭に「金額算定 可/不可」スイッチ。ラジオボタン8択（極めて優秀→物足りない）。金額算定ON時は各ボタン下に金額目安を表示 | 点数: 20/18/16/14/10/8/4/2。金額目安（ON時）: ≥10万/＜10万/≥7万/＜7万/≥4万/＜4万/≥1万/＜1万 |
| ②独創性 | ラジオボタン8択（極めて優秀→物足りない）。改善提案以外の表彰区分時は非表示 | 点数: 20/18/16/14/10/8/4/2 |
| ③努力工夫 | ラジオボタン8択（極めて優秀→物足りない）。改善提案以外の表彰区分時は非表示 | 点数: 20/18/16/14/10/8/4/2 |
| ④応用範囲 | ラジオボタン8択（極めて優秀→物足りない）。改善提案以外の表彰区分時は非表示 | 点数: 20/18/16/14/10/8/4/2 |
| 素点合計 | ※改善提案以外の表彰区分時は算出不要 | ①(a or b) + ② + ③ + ④ |
| 職能換算 | 自動算出 | 素点合計 × 職能係数（管理職: 0.85 / 一般: 1.00）。申請者の職位から社員マスタを参照してデフォルト設定 |
| 等級・賞 | 自動判定 | 区分ごとの判定テーブル参照、または表彰区分の賞 |
| 褒賞金額 | 自動算出 | 等級/賞から金額テーブル参照 |
| コメント | フリーテキスト | |
| おすすめ情報 | トグル（はい/いいえ）。デフォルトOFF | 評価者が「おすすめ」と判断した場合にON |
| 差戻ボタン | ボタン | 確認ポップアップ→NG通知 |
| 承認ボタン | ボタン | 確認ポップアップ→承認処理 |

## 4.5 申請状況確認導線 `[v10.1]`

申請者が自分の申請一覧と評価結果を確認するための導線。SharePointリストのカスタムビュー＋Column Formattingで実現し、Power Appsホーム画面の開発を不要とする。

### 4.5.1 SharePointリスト カスタムビュー

改善提案メインリストに「自分の申請」ビューを作成する。

| 設定項目 | 内容 |
|---------|------|
| ビュー名 | 自分の申請 |
| フィルタ | ApplicantEmail = [Me] |
| 並び替え | ID 降順（新しい順） |
| 表示列 | RequestID / Theme / Status / CompletionDate / FinalRewardAmount |

### 4.5.2 Column Formatting（列の書式設定）

#### 列レベル書式（全ビュー共通）

RequestID列にJSON書式設定を適用し、Power Apps閲覧画面へのリンクボタンを表示する。「自分の申請」「すべてのアイテム」ビューで使用される。

```json
{
  "$schema": "https://columnformatting.sharepointpnp.com/columnFormattingSchema.json",
  "elmType": "a",
  "txtContent": "@currentField",
  "style": {
    "color": "#0078d4",
    "text-decoration": "underline",
    "cursor": "pointer"
  },
  "attributes": {
    "href": "='https://apps.powerapps.com/play/{AppID}?RequestID=' + @currentField",
    "target": "_blank"
  }
}
```

> **注意**: `{AppID}` はPower AppsアプリのGUIDに置換すること。アプリ公開後に確定する。

#### ビューレベル書式：「自分の承認待ち」ビュー `[§13]`

「自分の承認待ち」ビューでは、列レベル書式をビューレベルで上書きし、Status列の値に基づいて承認画面へ直接遷移するパラメータを付与する。

| Status | 付与パラメータ | 遷移先 |
|--------|--------------|--------|
| 課長評価中 | `&EvalType=課長` | 評価画面（課長モード） |
| 部長評価中 | `&EvalType=部長` | 評価画面（部長モード） |
| 回覧中 | `&Mode=Reviewer` | 評価画面（回覧モード） |
| その他 | なし | 閲覧画面 |

```json
// ビューの CustomFormatter に設定（キーは列内部名）
{
  "RequestID": {
    "elmType": "a",
    "txtContent": "@currentField",
    "style": { "color": "#0078d4", "text-decoration": "underline", "cursor": "pointer" },
    "attributes": {
      "href": "='https://apps.powerapps.com/play/{AppID}?RequestID=' + @currentField + if([$Status] == '課長評価中', '&EvalType=課長', if([$Status] == '部長評価中', '&EvalType=部長', if([$Status] == '回覧中', '&Mode=Reviewer', '')))",
      "target": "_blank"
    }
  }
}
```

> Power Apps側の対応は不要。App.OnStart / App.StartScreen は既に `EvalType` / `Mode=Reviewer` パラメータに対応済み。

### 4.5.3 Power Apps URLパラメータ受け取り

App.OnStartにURLパラメータ処理を追加し、メールリンクやSharePointからの直接遷移を可能にする。

```
// URLパラメータで閲覧画面に直接遷移
If(
    !IsBlank(Param("RequestID")),
    Set(varViewRequestID, Param("RequestID"));
    Set(varViewMode, "View");
    Navigate(ViewScreen)
)
```

### 4.5.4 ユーザー導線

| 導線 | フロー |
|------|--------|
| SharePointから（自分の申請） | 改善提案メインリスト → 「自分の申請」ビュー → RequestIDクリック → Power Apps閲覧画面 |
| SharePointから（承認待ち） | 改善提案メインリスト → 「自分の承認待ち」ビュー → RequestIDクリック → Power Apps評価画面（Status に応じた画面） `[§13]` |
| メールから | 承認完了/差戻メールのリンク → Power Apps閲覧画面（評価結果表示） |
| アプリ内から | 申請フォームのプレビューボタン → 閲覧画面（従来通り） |

## 4.6 ★ ホーム画面（提案プラン）

| 機能 | 対象 | 詳細 |
|------|------|------|
| 自分の申請一覧 | 申請者 | ギャラリー表示、ステータスバッジ、タップで申請内容閲覧画面へ遷移 |
| 承認待ち一覧 | 課長/部長 | 未処理件数表示、タップで評価画面へ遷移 |
| ステータスフィルタ | 全ユーザー | 申請中/承認済/差戻などで絞り込み |
| 期間フィルタ | 全ユーザー | 日付範囲での絞り込み |

## 4.7 ★ 管理者画面（提案プラン）
